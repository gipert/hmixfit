Bootstrap: library
From: gipert/default/bat:1.0.0

%files
    src /opt/src/hmixfit

%post
    cd /opt/src/hmixfit
    make clean && make && make PREFIX=/opt install
    rm -r /opt/src

%environment
    export PATH="$PATH:/opt/bin"
